p#notice= notice
h1.page-header 商户请求列表

= paginate @collection
table.table.table-bordered.table-striped
  thead
    tr
      th 商户
      th order_id
      th trans_type
      th order_time
      th order_title
      th amount
      th fee
      th remote_ip
      th mac
      th status

  tbody
    - @collection.each do |p|
      tr
        td rowspan=2
          = p.client.name if p.client
          br/
          = p.org_id
        td= link_to p.order_id, p
        td= p.trans_type
        td= p.order_time
        td= p.order_title
        td= p.amount
        td= p.fee
        td= p.remote_ip
        td= p.mac
        td
          = p.status
          | &nbsp;
          - if p.status < 7
            = link_to '通知', send_notify_client_payment_path(p), remote: true

      tr
        td colspan=11
          - if p.img_url
            strong 二维码url:
            = p.img_url
          - if p.card_no
            strong 银行卡：
            = p.card_no
            strong 名字：
            = p.card_holder_name
          br/
          strong Notify：
          = p.notify_url
          | &nbsp;
          strong Callback：
          = p.callback_url
